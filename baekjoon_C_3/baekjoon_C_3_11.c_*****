#include <stdio.h>

int main(){
    int a=0, b=0;
    while (1){
        scanf("%d %d", &a, &b);
        if (a==0 && b==0){
            break;
        }
        else{
            printf("%d\n", a+b);
        }
    }
    return 0;
}

// for문에 대한 설명
// for문에서는 (초기식; 조건식; 증감식) 이렇게 ;로 구분된 3개가 와야 함
// 만약 무한 루프를 씌우고 싶다면 for (;;)로 진행

// while문에 대한 설명
// while문에서는 논리식을 그대로 사용해도 됨.
// 단 그 논리 식이 참이어야만 작동함
// 만약 무한 루프를 씌우고 싶다면 while(1)로 진행

// 둘다 그만하고 싶으면 if(조건) break;로 루프를 끝냄
